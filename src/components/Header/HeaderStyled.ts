import { styled } from "styled-components";

export const HeaderStyled = styled.header`
    position: sticky;
    width: 100%;
    height: 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    z-index: 1;
`;
